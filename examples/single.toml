# Single text synthesis — Qwen3-TTS (Base)
# Output: ./single.wav

format = "wav"
model = "mlx-community/Qwen3-TTS-12Hz-0.6B-Base-4bit"

[params]
voice = "Ryan"
lang_code = "auto"
speed = 1.0
temperature = 0.9
top_k = 50
top_p = 1.0
repetition_penalty = 1.05

[script]
text = """
Hello, world. This is a test of the mlx-tts wrapper.
"""
